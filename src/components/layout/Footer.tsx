export default function Footer() {
  return (
    <footer className="bg-red-700 text-white text-center py-4 text-sm mt-auto">
      <p>© {new Date().getFullYear()} Blood Donor Finder. Saving lives, one donation at a time. 🩸</p>
    </footer>
  );
}
